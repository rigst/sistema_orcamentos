name: CD

# TEMPORÁRIO: workflow_dispatch em vez de workflow_run, para testar de ponta
# a ponta antes de confiar no gatilho automático (ver RUNBOOK.md do rigst/ci,
# seção 7.5).

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: rigst/ci/.github/workflows/deploy-django.yml@26c32b5e77661715f25db958b20174b8f172f017  # v1
    secrets:
      CD_SSH_KEY: ${{ secrets.CD_SSH_KEY }}
    with:
      ssh-host: "orcamentos.stolben.com"
